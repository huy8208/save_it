import 'package:dio/dio.dart';

const String baseUrl = 'https://jsonplaceholder.typicode.com/posts/';

class ApiDio {
  // final Dio dio = Dio();
  dynamic getSomething() async {
    try {
      // final Future<Response<dynamic>> reponses = dio.get(baseUrl);
      // return reponses;
    } catch (_) {}
  }
}
